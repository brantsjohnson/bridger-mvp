{
  "buildCommand": "npm run build",
  "outputDirectory": "apps/dist",
  "installCommand": "npm install && cd apps && npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
} 